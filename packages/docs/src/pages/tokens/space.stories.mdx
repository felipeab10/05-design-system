import { Meta } from '@storybook/addon-docs'
import { TokensGrid } from '../../components/TokensGrid'
import { space } from '@felipeab10-ignite-ui/tokens'

<Meta title="Tokens/Space"/>

# Space

Esses são os espaços utilizadas no ignite UI.

<TokensGrid tokens={space} hasPixelValue/>